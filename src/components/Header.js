import React from "react";

const Header = props => {
  return (
    <header className="App-header">
      <h2>Employee Directory</h2>
    </header>
  );
};

export default Header;


// this is the header of the app, styling in app.css